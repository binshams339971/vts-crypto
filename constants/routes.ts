const loginRoute = '/login';
const home = '/'
const dashboard = '/dashboard'
const order = '/dashboard/order'
const tracking = '/dashboard/tracking'
const transaction = '/dashboard/transaction'
const checkout = '/dashboard/checkout'

export { loginRoute, home, dashboard, order, tracking, transaction, checkout }